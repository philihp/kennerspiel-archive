version: 0.0
os: linux
files:
  - source: /
    destination: /srv/kennerspiel/
hooks:
  ApplicationStart:
    - location: bin/start.sh
      runas: ec2-user
  ValidateService:
    - location: bin/verify.sh
      runas: ec2-user
